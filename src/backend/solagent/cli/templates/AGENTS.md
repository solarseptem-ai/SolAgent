# {{name}} — Agent Instructions

This project uses SolarSeptem Agent Framework.

## Configuration

Edit `solagent.yaml` to configure providers, agents, and MCP servers.

## Running

```bash
solagent run
```

## Adding Tools

Add tools to your agent in `agents.yaml` under the `tools` field.